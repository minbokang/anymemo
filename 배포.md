# AnyMemo 배포 가이드

**English:** [DEPLOY.md](./DEPLOY.md)

Vite + React + Supabase 메모 앱을 **웹 → 데스크톱(Tauri) → (추후) 모바일 스토어** 순으로 배포합니다.

| 단계 | 플랫폼 | 상태 |
|------|--------|------|
| 1 | 웹 + PWA (Vercel) | ✅ 운영: https://anymemo.vercel.app |
| 2 | Windows / macOS (Tauri v2) | ✅ [Release v0.1.0](https://github.com/minbokang/anymemo/releases/tag/v0.1.0) |
| 3 | App Store / Play Store (Capacitor) | ⏸️ 추후 진행 |

### 다운로드 (v0.1.0)

| OS | 설치 파일 |
|----|-----------|
| **macOS** (Apple Silicon) | [DMG](https://github.com/minbokang/anymemo/releases/download/v0.1.0/AnyMemo_0.1.0_aarch64.dmg) · [ZIP](https://github.com/minbokang/anymemo/releases/download/v0.1.0/AnyMemo_0.1.0_macos_aarch64.zip) |
| **Windows** (x64) | [MSI](https://github.com/minbokang/anymemo/releases/download/v0.1.0/AnyMemo_0.1.0_x64_en-US.msi) · [EXE](https://github.com/minbokang/anymemo/releases/download/v0.1.0/AnyMemo_0.1.0_x64-setup.exe) |

---

## 공통: 환경 변수

웹·데스크톱 빌드 모두 **빌드 시점**에 아래 값이 번들에 들어갑니다.

| 변수 | 용도 |
|------|------|
| `VITE_SUPABASE_URL` | Supabase 프로젝트 URL |
| `VITE_SUPABASE_ANON_KEY` | anon public key |

- 로컬: `.env.local` (git 제외)
- Vercel: Project → Settings → Environment Variables
- Tauri: `npm run build` / `npm run tauri:build` 전에 같은 `.env.local`이 있어야 함

Supabase 대시보드 **Authentication → URL Configuration**에 프로덕션 웹 주소(`https://anymemo.vercel.app` 등)가 Redirect URLs에 포함되어 있는지 확인합니다.

---

## 1단계: 웹 & PWA (Vercel)

### 빌드 확인

```bash
npm run build
```

`dist/`에 `index.html`, `assets/`, `registerSW.js`, `manifest.webmanifest`가 생성되는지 확인합니다.

### 배포

```bash
npx vercel deploy --prod --yes
```

또는 GitHub `main` 푸시 후 Vercel 자동 배포.

### PWA

- 모바일 Safari/Chrome에서 사이트 접속 → **홈 화면에 추가**
- 앱 내 상단 설치 배너로도 안내 (스토어 없이 앱처럼 사용)

### 사용자 배포 (직접 다운로드 링크)

웹만 쓸 때는 URL 공유로 충분합니다. 로그인 화면·도움말에서 `https://anymemo.vercel.app` 안내.

---

## 2단계: 데스크톱 (Tauri v2) — 지금 진행

### 사전 요구 사항

1. **Rust** — https://rustup.rs (한 번만 설치)
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
   source "$HOME/.cargo/env"   # 현재 터미널에 PATH 반영
   rustc --version
   ```
   새 터미널을 열면 `~/.zshrc`에 rustup이 PATH를 넣어 둡니다.
2. **플랫폼별 도구**
   - **macOS**: Xcode Command Line Tools (`xcode-select --install`)
   - **Windows**: [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) + WebView2 (보통 Windows 11에 포함)

자세한 목록: https://v2.tauri.app/start/prerequisites/

### 프로젝트 구조 (이미 초기화됨)

- `src-tauri/` — Rust 셸, `tauri.conf.json`
- 웹 자원: `../dist` (Vite 빌드 결과)
- 개발 서버: `http://localhost:5173`

### 개발 실행

```bash
npm run tauri:dev
```

Vite dev 서버 + AnyMemo 데스크톱 창이 함께 뜹니다. 웹만 개발할 때는 기존처럼 `npm run dev`.

### 프로덕션 설치 파일 빌드

```bash
npm run tauri:build
```

**결과물** (`src-tauri/target/release/bundle/`):

| OS | 파일 예시 |
|----|-----------|
| macOS | `AnyMemo.app` (Applications에 드래그), `.dmg` (생성 실패 시 `.app`만 사용) |
| Windows | `.msi`, `.exe` (Windows PC에서 빌드) |

첫 빌드는 Rust 의존성 컴파일로 **수 분** 걸릴 수 있습니다.

`.dmg` 생성이 실패해도 `bundle/macos/AnyMemo.app`이 있으면 더블클릭으로 실행·`/Applications`로 복사하면 됩니다.

### 아이콘 갱신

`public/favicon.svg` 변경 후:

```bash
npx tauri icon public/favicon.svg
```

### 설치 파일 배포 (스토어 없이)

App Store / Microsoft Store **없이** 배포하는 일반적인 방법:

1. **GitHub Releases** (권장)
   - 태그 `v0.1.0` 생성
   - `bundle/` 안의 `.dmg` / `.msi` 업로드
   - 릴리스 노트에 macOS / Windows 구분
2. **다운로드 페이지**
   - Vercel 정적 페이지 `/download` 또는 README 링크
   - OS별 버튼 → Releases 최신 asset URL
3. **로그인 화면** (`PlatformInstallGuide`)
   - 브라우저 URL · PWA 안내 · Mac/Windows 다운로드 · GitHub Releases 링크
   - 직접 파일 URL이 있으면 `.env`에 설정:
     - `VITE_DOWNLOAD_MAC_DMG` — Mac `.dmg` 직링크
     - `VITE_DOWNLOAD_WINDOWS_MSI` — Windows `.msi` 직링크
   - 없으면 `releases/latest`로 연결됨

### macOS 배포 시 참고

- 서명·공증(notarization) 없이 배포하면 Gatekeeper 경고가 뜰 수 있습니다.
- 배포용: Apple Developer ID + `codesign` + `notarization` (추후 정식 배포 시).

### Windows — GitHub Actions (Mac 없이 빌드)

워크플로: [`.github/workflows/release-windows.yml`](./.github/workflows/release-windows.yml)

| 항목 | 내용 |
|------|------|
| 러너 | `windows-latest` |
| 트리거 | `v*` 태그 push (예: `v0.1.1`) 또는 Actions에서 **Run workflow** |
| 결과 | 해당 태그의 GitHub Release에 `.msi` / `.exe` 업로드 |

**최초 1회 설정**

1. GitHub 저장소 **Settings → Secrets and variables → Actions**에 추가:
   - `VITE_SUPABASE_URL` (Vercel과 동일)
   - `VITE_SUPABASE_ANON_KEY` (anon public key)
2. **Settings → Actions → General → Workflow permissions** → **Read and write permissions**

**실행 예**

```bash
# 버전 올린 뒤 (tauri.conf.json version 등)
git tag v0.1.1
git push origin v0.1.1
```

기존 `v0.1.0` 릴리스에 Windows만 붙이려면 Actions 탭에서 workflow를 수동 실행하고 tag에 `v0.1.0` 입력.

MSI가 올라오면 README·Vercel에 `VITE_DOWNLOAD_WINDOWS_MSI` 직링크를 넣을 수 있습니다.

### Windows 배포 시 참고

- 코드 서명 인증서가 있으면 SmartScreen 경고를 줄일 수 있습니다.

### 버전 올리기

1. `src-tauri/tauri.conf.json`의 `version`
2. (선택) `package.json` `version`
3. `npm run tauri:build` 후 Releases에 새 파일 업로드

---

## 3단계: 모바일 스토어 (Capacitor) — 추후

Play Store / App Store 심사·개발자 계정·스크린샷 등 준비 후 진행합니다.

**당장은 PWA(홈 화면 추가)** 로 iOS/Android를 커버하는 것을 권장합니다.

초기화 예시 (나중에):

```bash
npm install @capacitor/core @capacitor/cli
npx cap init "AnyMemo" "com.anymemo.app" --web-dir=dist
npm install @capacitor/android @capacitor/ios
npx cap add android
npx cap add ios
```

웹 수정 후 반영:

```bash
npm run build
npx cap sync
npx cap open android   # 또는 ios
```

---

## 배포 전 체크리스트

1. **Supabase RLS** — `memos` 테이블 본인 데이터만 접근
2. **Auth Redirect URLs** — Vercel 프로덕션 URL 등록
3. **마이그레이션** — `npm run supabase:push` (필요 시)
4. **데스크톱 빌드** — `.env.local` 포함 상태에서 `npm run tauri:build` 성공 여부
5. **아이콘** — `npx tauri icon public/favicon.svg` 후 재빌드

---

## 저장소·호스팅

- Git: https://github.com/minbokang/anymemo.git
- 웹: Vercel (`anymemo.vercel.app`)
- 데스크톱: GitHub Releases 등에 설치 파일 게시 (위 2단계)
