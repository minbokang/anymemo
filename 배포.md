# 🚀 실시간 동기화 메모장 크로스 플랫폼 배포 가이드 (배포.md)

본 문서는 Vite + React + Supabase 기반의 메모장 앱을 웹(PWA), 데스크톱(Tauri), 모바일(Capacitor) 플랫폼으로 각각 빌드하고 배포하는 절차를 정의합니다.

---

## 🌐 1단계: 웹 호스팅 & PWA 배포 (Vercel)

가장 먼저 진행하며, 모바일(PWA) 및 향후 앱 패키징의 기준이 되는 배포입니다.

### 1. 웹 프로덕션 빌드 테스트
```bash
npm run build
```
- 빌드 완료 후 `dist/` 폴더가 정상적으로 생성되는지 확인합니다.
- PWA 매니페스트와 서비스 워커(`registerSW.js`)가 누락 없이 포함되었는지 검증합니다.

### 2. Vercel 배포 과정
1. [Vercel](https://vercel.com) 계정 로그인 후 GitHub 저장소와 프로젝트 연동
2. **Environment Variables (환경 변수)** 설정 필수:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
3. [Deploy] 버튼 클릭 후 발급된 `https://vercel.app` 주소 확인
4. 모바일 브라우저(Safari/Chrome)로 접속하여 **"홈 화면에 추가(PWA)"** 기능 작동 여부 테스트

---

## 💻 2단계: 데스크톱 앱 배포 (Tauri v2)

윈도우 및 맥 사용자를 위한 독립형 설치 파일(`.exe`, `.dmg`)을 빌드합니다.

### 1. Tauri 환경 초기화 (최초 1회)
```bash
npm install @tauri-apps/cli@next @tauri-apps/api@next
npx tauri init
```
- 설정 유의점: 웹 자원 경로(Web assets path)를 `../dist`로 지정합니다.
- 개발 서버 연결 주소(Dev server URL)는 `http://localhost:5173`으로 지정합니다.

### 2. 플랫폼별 프로덕션 빌드
```bash
npx tauri build
```
- **결과물 위치**: `src-tauri/target/release/bundle/`
  - **Windows**: `.exe` 및 `.msi` 설치 파일 생성
  - **macOS**: `.dmg` 및 `.app` 애플리케이션 생성
- *팁*: GitHub Actions를 이용하면 윈도우/맥 설치 파일을 클라우드에서 자동으로 빌드하도록 자동화할 수 있습니다.

---

## 📱 3단계: 모바일 스토어 배포 (Capacitor)

구글 플레이스토어 및 애플 앱스토어에 정식 출시하기 위한 네이티브 프로젝트 빌드입니다.

### 1. Capacitor 초기화 및 플랫폼 추가 (최초 1회)
```bash
# Capacitor 설치 및 초기화
npm install @capacitor/core @capacitor/cli
npx cap init "Sync Memo" "com.yourdomain.syncmemo" --web-dir=dist

# 각 모바일 플랫폼 추가
npm install @capacitor/android @capacitor/ios
npx cap add android
npx cap add ios
```

### 2. 코드 수정 후 모바일 동기화 프로세스 (수정 시마다 반복)
웹 코드(`src/`) 수정 후 모바일 앱에 반영할 때 매번 거치는 명령어입니다.
```bash
npm run build
npx cap sync
```

### 3. 네이티브 빌드 도구 연동
```bash
# 안드로이드 스튜디오 실행 (Android 앱 .apk/.aab 빌드용)
npx cap open android

# Xcode 실행 (iOS 앱 .ipa 빌드용 *Mac 필수*)
npx cap open ios
```
- 각 IDE(Android Studio / Xcode)가 실행되면 상단 메뉴의 **Build ➡️ Build Bundle/APK** 또는 **Archive**를 선택하여 최종 스토어 제출용 파일을 추출합니다.

---

## ⚠️ 배포 전 최종 체크리스트

1. **Supabase RLS (보안)**: 대시보드에서 `memos` 테이블의 Row Level Security 정책이 활성화되어 다른 사람이 내 메모를 보지 못하게 막았는지 확인합니다.
2. **Supabase 인증 리다이렉트**: 구글 로그인 등을 쓸 경우, Supabase Auth 설정의 Redirect URLs에 Vercel 프로덕션 도메인 주소를 추가했는지 확인합니다.
3. **아이콘 리소스**: `src-tauri/icons` 및 Capacitor 자산 폴더에 앱 아이콘이 규격별로 정상 교체되었는지 체크합니다.



git : https://github.com/minbokang/anymemo.git

vercel : github 계정으로 가입함.