# 📝 실시간 동기화 메모장 앱 개발 계획서 (Cursor AI 가이드용)

본 프로젝트는 단 하나의 프론트엔드 코드(Vite + React)를 기반으로, Supabase 백엔드를 연동하고, 최종적으로 Tauri와 Capacitor를 통해 웹, 모바일, 데스크톱(Windows/Mac) 전 플랫폼을 지원하는 가볍고 빠른 실시간 동기화 메모장 앱을 구축합니다.

---

## 🛠️ 기술 스택 (Tech Stack)
- **Frontend**: Vite + React + Tailwind CSS (경량화 및 고속 구동)
- **Backend/DB**: Supabase (인증, PostgreSQL, Realtime 기능 활용)
- **Local Storage**: IndexedDB (오프라인 모드 캐싱용, localStorage에서 자동 이전)
- **Desktop Packaging**: Tauri v2 (수 MB 크기의 초경량 설치 파일 앱 빌드)
- **Mobile Packaging**: Capacitor (웹뷰 기반 iOS/Android 패키징)

---

## 📅 개발 로드맵 (Roadmap)

### 🟩 [1단계] 개발 환경 세팅 & Supabase 기본 연동
- [x] Node.js 환경 확인 및 Vite + React 기본 프로젝트 생성
- [x] Tailwind CSS 스타일링 환경 세팅
- [x] `.env.local` 파일에 Supabase API URL 및 Anon Key 설정
- [x] `src/supabaseClient.js` 작성 및 클라이언트 초기화 코드 검증

### 🟩 [2단계] 데이터베이스(DB) 설계 및 RLS 보안 설정
- [x] Supabase 대시보드 내 `memos` 테이블 생성 (UUID, userId, title, content, updated_at 등)
- [x] **Row Level Security (RLS) 활성화**: 로그인한 사용자 본인의 데이터만 접근 가능한 Policy 설정
- [x] 회원가입 및 이메일/비밀번호 로그인(Authentication) 로직 구현

### 🟩 [3단계] 메모장 핵심 기능 및 실시간 동기화(Realtime) 구현
- [x] 메모 목록 조회(Read), 새 메모 작성(Create), 메모 수정(Update) 및 삭제(Delete) UI 구성
- [x] **디바운싱(Debouncing) 적용**: 타이핑이 멈추고 0.5초 뒤 자동으로 Supabase에 저장되는 기능
- [x] **Supabase Realtime(채널 구독) 연동**: 다른 기기에서 수정 시 화면이 실시간 양방향 동기화되는 로직 구현

### 🟩 [4단계] 오프라인 캐싱 & 예외 처리 (배포 필수 관문)
- [x] 앱 구동 시 기기 로컬 저장소에 메모 데이터를 캐싱하여 오프라인에서도 작동하도록 구현
- [x] 오프라인 상태에서 작성/수정한 메모를 온라인 전환 시 서버와 동기화(Sync)하는 병합 로직 작성

### 🟩 [5단계] 크로스 플랫폼 패키징 (Tauri & Capacitor)
- [x] 웹앱 빌드 후 Vercel을 통한 1차 무료 웹 배포 및 테스트
- [x] **Tauri 세팅**: `src-tauri/` 초기화, `npm run tauri:dev` / `tauri:build` 스크립트 (`배포.md` 2단계)
- [ ] **Tauri 배포**: GitHub Releases에 `.dmg` / `.msi` 게시, (선택) 로그인 화면 다운로드 링크
- [ ] **Capacitor 세팅**: ⏸️ App Store / Play Store는 추후 — 당분간 PWA로 모바일 커버

---

## 🤖 Cursor AI 전용 프롬프트 지시어 (Prompt Instructions)

Cursor로 코딩을 진행할 때 아래 컨텍스트를 참고하여 코드를 생성해 주세요.

1. **Keep it light**: 윈도우 메모장 같은 극도의 가벼움과 빠른 반응 속도를 유지하는 컴포넌트 구조를 지향합니다.
2. **No Backend Server**: Next.js나 Node.js 서버 기능은 일체 사용하지 않으며, 모든 DB 요청과 인증은 클라이언트에서 `supabaseClient`를 통해 직접 호출합니다.
3. **Type-Safe & Clean**: 자바스크립트 가독성을 높이고, 상태 관리는 React의 기본 Context API나 가벼운 상태관리 라이브러리를 선호합니다.
